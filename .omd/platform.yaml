# Platform-specific configuration
platforms:
  azuredevops:
    # Branch policies
    branch_policies:
      minimum_reviewers: 1
      require_work_item_linking: true
      build_validation: true
      path_filters:
        - "/*"
