# Repository metadata configuration format
# Languages used in this repository
languages:
  - terraform
  - python

# CI/CD platform where this repository resides  
platform: github

# Cloud providers this repository targets
cloud_providers:
  - aws
  - azure

# Repository type/category
type: terraform-module