---
applyTo: "**/*.ts,**/*.tsx,**/*.js,**/*.jsx,**/package.json,**/tsconfig.json"
---

# TypeScript Development Instructions

This document provides comprehensive guidelines for TypeScript and JavaScript development in the {{ repo_name }} project.

## Architecture Guidelines

### Project Structure

Organize TypeScript projects with clear separation of concerns:

```
{{ repo_name }}/
├── src/
│   ├── components/    # Reusable UI components
│   ├── services/      # API and business logic
│   ├── types/         # TypeScript type definitions
│   ├── utils/         # Helper functions
│   └── __tests__/     # Unit tests
├── package.json       # Dependencies and scripts
├── tsconfig.json      # TypeScript configuration
└── README.md         # Project documentation
```

## Implementation Standards

### Type Definitions

Use comprehensive TypeScript interfaces:

```typescript
interface Entity {
  id: string;
  name: string;
  type: string;
  createdAt: Date;
  updatedAt: Date;
}

type Status = 'active' | 'inactive' | 'pending';
```

### Error Handling

Implement structured error management:

```typescript
class ApiError extends Error {
  constructor(
    message: string,
    public statusCode: number
  ) {
    super(message);
    this.name = 'ApiError';
  }
}
```

## Quality Assurance

### Testing Standards

Use Jest for unit testing:

```typescript
describe('{{ repo_name | title }}Service', () => {
  it('should handle API responses correctly', () => {
    // Test implementation
  });
});
```

### Code Quality

Configure ESLint and Prettier for consistent code style:

```json
{
  "extends": ["@typescript-eslint/recommended", "prettier"],
  "rules": {
    "@typescript-eslint/no-unused-vars": "error"
  }
}
```

## Development Workflow

### Build Configuration

TypeScript configuration for modern development:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "strict": true,
    "jsx": "react-jsx"
  }
}
```

### Package Management

Essential dependencies for TypeScript development:

- TypeScript compiler
- ESLint and Prettier
- Jest for testing
- Type definitions

## Documentation Standards

Use JSDoc comments for comprehensive documentation:

```typescript
/**
 * Retrieves user data from the API
 * @param userId - The unique identifier for the user
 * @returns Promise resolving to user data
 */
async function fetchUser(userId: string): Promise<User | null> {
  // Implementation
}
```

This guide ensures consistent TypeScript development practices across the {{ repo_name }} project.